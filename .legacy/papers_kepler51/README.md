# Legacy paper-local geotrans copy

Monolithic `geotrans.py` that used to sit next to Contours notebooks. Contours now imports
`geotrans.geotrans` from `pipeline/geotrans/` (with `pipeline/` on `sys.path`).
